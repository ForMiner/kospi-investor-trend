# Both drivers write this file — PowerShell with CRLF, Python on Linux with LF.
# Pin it to LF so a run from the other platform doesn't rewrite all 290 rows.
data/kospi_investor_daily.csv text eol=lf

# Written by grid_bot.py on every fill; same reason.
data/grid_trades.csv text eol=lf
